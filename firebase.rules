rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null
        && request.auth.token.email in [
          'admin@newculturetrends.com'
        ];
    }

    match /items/{itemId} {
      allow read: if true;
      allow create, delete: if isAdmin();
      // Allow anonymous vote updates - allow increment or decrement by 1
      // Only allow updating votes field (updatedAt is a transform, not checked)
      allow update: if isAdmin() ||
        (
          'votes' in request.resource.data &&
          request.resource.data.votes is int &&
          request.resource.data.votes >= 0 &&
          (
            // Allow if votes field doesn't exist in current document (new item)
            !('votes' in resource.data) ||
            // Allow increment by 1
            (request.resource.data.votes == resource.data.votes + 1) ||
            // Allow decrement by 1 (including going from 1 to 0)
            (request.resource.data.votes == resource.data.votes - 1) ||
            // Allow if current votes is null/undefined and new is 0 or 1
            (resource.data.votes == null && request.resource.data.votes <= 1)
          )
        );
    }

    match /itemVotes/{voteId} {
      // Allow anyone to read, create, update, and delete their own votes (IP-based, no auth required)
      // Note: Duplicate prevention is handled by transaction logic in the app
      // Transactions may use 'set' which can be treated as update
      allow read: if true;
      allow create, update, delete: if true;
    }

    match /polls/{pollId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /pollVotes/{voteId} {
      // Allow anyone to read and create/update poll votes (IP-based, no auth required)
      // Note: Duplicate prevention is handled by transaction logic in the app
      // Transactions may use 'set' which can be treated as update
      allow read: if true;
      allow create, update: if true;
      allow delete: if isAdmin();
    }

    match /newsletter/{docId} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }
  }
}

